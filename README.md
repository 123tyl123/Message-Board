# 📝 留言板客户端 (Message Board Client)

一个基于 Vue 3 和 Vite 构建的现代化全栈留言板前端应用，提供简洁直观的用户界面，支持留言的发布、查看和删除功能。

## ✨ 功能特性

- 📝 **发布留言** - 输入内容快速发布新留言
- 📋 **查看留言** - 实时显示所有用户留言列表
- 🗑️ **删除留言** - 点击按钮删除不需要的留言
- 🎨 **响应式设计** - 适配各种屏幕尺寸
- ⚡ **热更新开发** - 基于 Vite 的快速开发体验
- 🌐 **现代化界面** - 简洁美观的 Material Design 风格

## 🛠️ 技术栈

- **前端框架**: Vue 3 (Composition API)
- **构建工具**: Vite
- **HTTP 客户端**: Axios
- **开发语言**: JavaScript (ES6+)
- **样式**: CSS3 (Scoped)
- **开发工具**: Vue DevTools

## 📋 环境要求

- Node.js >= 20.19.0 或 >= 22.12.0
- npm 或 yarn

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

开发服务器将在 `http://localhost:5173` 启动

### 3. 构建生产版本

```bash
npm run build
```

### 4. 预览生产版本

```bash
npm run preview
```

## 📡 API 接口

客户端与后端服务器 (`http://localhost:3000`) 进行通信：

### 获取留言列表
```http
GET /api/messages
```

**响应格式**:
```json
{
  "code": 0,
  "data": [
    {
      "id": 1,
      "content": "欢迎来到留言板！",
      "time": "2023-11-20"
    }
  ]
}
```

### 发布新留言
```http
POST /api/messages
Content-Type: application/json

{
  "content": "留言内容"
}
```

**响应格式**:
```json
{
  "code": 0,
  "msg": "发送成功",
  "data": {
    "id": 2,
    "content": "留言内容",
    "time": "2023-11-21"
  }
}
```

### 删除留言
```http
DELETE /api/messages/:id
```

**响应格式**:
```json
{
  "code": 0,
  "msg": "删除成功"
}
```

## 📁 项目结构

```
client/
├── public/                 # 静态资源
│   └── favicon.ico        # 网站图标
├── src/                   # 源代码目录
│   ├── assets/           # 资源文件
│   ├── components/       # Vue 组件
│   ├── App.vue          # 主应用组件
│   └── main.js          # 应用入口
├── index.html            # HTML 模板
├── package.json          # 项目配置
├── vite.config.js        # Vite 配置
└── README.md            # 项目文档
```

## 🎯 核心功能实现

### 状态管理
- 使用 Vue 3 `ref()` 进行响应式状态管理
- `messageList` 存储留言列表数据
- `inputContent` 存储用户输入内容

### 网络请求
- 使用 Axios 库进行 HTTP 请求
- 统一的 API 基础地址配置
- 错误处理和用户反馈

### 用户交互
- Enter 键快速发布留言
- 点击按钮删除指定留言
- 表单验证防止空内容提交

## 🔧 开发工具推荐

- **IDE**: Visual Studio Code
- **扩展**: Vue Language Features (Volar)
- **浏览器**: Vue.js DevTools
- **代码格式化**: ESLint + Prettier

## 🚀 部署说明

### 构建生产版本
```bash
npm run build
```

构建完成后，`dist/` 目录包含所有静态文件，可以部署到任何静态文件服务器。

### 部署选项
- **Vercel**: 连接 GitHub 仓库自动部署
- **Netlify**: 拖拽 `dist` 文件夹或连接仓库
- **GitHub Pages**: 通过 GitHub Actions 自动部署
- **传统服务器**: 将 `dist` 内容上传到 Web 服务器

## 🤝 相关项目

- [留言板服务器](https://github.com/your-username/message-board-server) - Express.js 后端 API

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系方式

如有问题或建议，请创建 Issue 或联系项目维护者。

---

⭐ 如果这个项目对您有帮助，请给个 Star 支持一下！
